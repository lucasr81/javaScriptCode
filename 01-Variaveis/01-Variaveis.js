
/*
Variaveis
Autor: Lucas Rocha
Data:  30/05/2023
*/

let idade = 26; // Criando variavel 
console.log(idade);


idade = 30; // Variavel do tipo number

let numero = 50; // number
let altutura = 1.77; // number
let nome = "Walisson" //string 
let estudando = true; // booleano (Verdadeiro ou falso)
console.log(idade, typof); // Imprimido o tipo da variavel
console.log(numero);
console.log(alutura);
console.log(nome);
console.log(estudando);

let outra;  // variavel não definida (undefined), ela não recebe nenhum valor 
console.log(outra)

let nula = null; // uma variavel nula
console.log(nula)

const constante = 100 // Essa variavel não pode ser alterada
// constante = 20 Aqui dara erro, Não pode fazer atribuição em uma variavel constante

// constante = 20 Aqui dara erro, pis foi alterada, uma constante não pode ser alterada

/*
Existem três tipos de declarações em JavaScript.

var
Declara uma variável, opcionalmente, inicializando-a com um valor. Não é mais recomendavel usar o Var, e sim o let

Experimental let
Declara uma variável local de escopo do bloco, opcionalmente, inicializando-a com um valor.

Experimental const
Declara uma constante de escopo de bloco, apenas de leitura.
fonte documentação: Mdn Font Docs
https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Guide/Grammar_and_types 
*/
