//Variaveis//
let idade = 26; // Criando variavel 
console.log(idade);


idade = 30; // Variavel do tipo number

let numero = 50; // number
let alutura = 1.77; // number
let nome = "Walisson" //string 
let estudando = true; // booleano (Verdadeiro ou falso)
console.log(idade, typeof idade); // imprimindo o tipo da variavel
console.log(numero);
console.log(alutura);
console.log(nome);
console.log(estudando);
let outra;  // variavel não definida (undefined), ela não recebe nenhum valor 
console.log(outra)

let nula = null; // uma variavel nula
console.log(nula)

const constante = 100 // Essa variavel não pode ser alterada
// constante = 20 Aqui dara erro, Não pode fazer atribuição em uma variavel constante
